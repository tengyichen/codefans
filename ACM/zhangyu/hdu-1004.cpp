#include<stdio.h>
#include<stdlib.h>

int main()
{
	int t, n;
	char color[16];
	int res[16][26];
	int i, j, k, len, tmp;
	
	scanf("%d", &t);
	for(i = 0; i < t; i++){
		scanf("%d", &n);
		memset(res, 0, sizeof(res));
		for(j = 0; j < n; j++){
			scanf("%s", color);
			len = strlen(color);
			
			}
		}
		
	}
	return 0;
}
